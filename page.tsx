'use client'

import { useEffect, useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, ArrowRight, Check, Sparkles, X } from 'lucide-react'

type ModalType = 'join' | 'learn' | null

function ProgramModal({ type, onClose }: { type: Exclude<ModalType, null>; onClose: () => void }) {
  const isJoin = type === 'join'
  const [submitted, setSubmitted] = useState(false)

  useEffect(() => {
    const handleKeyDown = (event: KeyboardEvent) => { if (event.key === 'Escape') onClose() }
    document.addEventListener('keydown', handleKeyDown)
    return () => document.removeEventListener('keydown', handleKeyDown)
  }, [onClose])

  return <div className="fixed inset-0 z-50 grid place-items-center bg-background/80 px-4 py-6 backdrop-blur-sm" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose() }}><section aria-labelledby={`${type}-modal-title`} aria-modal="true" className="glass relative max-h-[90vh] w-full max-w-lg overflow-y-auto rounded-2xl p-6 shadow-2xl sm:p-8" role="dialog"><button aria-label="Close modal" className="focus-ring absolute right-4 top-4 grid size-9 place-items-center rounded-lg text-muted hover:bg-secondary hover:text-foreground" onClick={onClose}><X size={18} /></button>{submitted ? <div className="py-8 text-center"><div className="mx-auto grid size-12 place-items-center rounded-full bg-primary text-primary-foreground"><Check size={22} /></div><h2 id="join-modal-title" className="mt-5 text-2xl font-semibold">You&apos;re on the list.</h2><p className="mt-3 leading-7 text-muted">Thanks for your interest. Marcus&apos; team will follow up with next steps.</p><button className="focus-ring mt-6 rounded-lg border border-border px-4 py-2 text-sm font-semibold hover:bg-secondary" onClick={onClose}>Close</button></div> : isJoin ? <><p className="eyebrow">Early access</p><h2 id="join-modal-title" className="mt-3 text-3xl font-semibold tracking-[-.03em]">Join the ClearTrade program.</h2><p className="mt-3 leading-7 text-muted">Get a focused walkthrough, early product access, and a direct line to the team shaping the review workflow.</p><form className="mt-6 grid gap-4" onSubmit={(event) => { event.preventDefault(); setSubmitted(true) }}><input aria-label="Your name" required placeholder="Your name" className="focus-ring h-12 rounded-lg border border-input bg-secondary/40 px-4 text-foreground placeholder:text-muted-foreground" /><input aria-label="Work email" required type="email" placeholder="Work email" className="focus-ring h-12 rounded-lg border border-input bg-secondary/40 px-4 text-foreground placeholder:text-muted-foreground" /><input aria-label="Company" required placeholder="Company" className="focus-ring h-12 rounded-lg border border-input bg-secondary/40 px-4 text-foreground placeholder:text-muted-foreground" /><button className="focus-ring inline-flex h-12 items-center justify-center gap-2 rounded-lg bg-primary px-5 text-sm font-semibold text-primary-foreground">Join the program <ArrowRight size={16} /></button></form></> : <><p className="eyebrow">Why join</p><h2 id="learn-modal-title" className="mt-3 text-3xl font-semibold tracking-[-.03em]">A smarter way to move review forward.</h2><p className="mt-3 leading-7 text-muted">The ClearTrade program is built for teams who want less friction without compromising control.</p><ul className="mt-6 grid gap-4">{['See the workflow before it is broadly available.', 'Turn real review bottlenecks into product improvements.', 'Get practical guidance from compliance and operations specialists.', 'Build a stronger, more consistent operating rhythm.'].map((reason) => <li key={reason} className="flex gap-3 text-sm leading-6 text-muted"><Check className="mt-1 shrink-0 text-primary" size={16} /> <span>{reason}</span></li>)}</ul><button className="focus-ring mt-7 inline-flex h-12 items-center justify-center rounded-lg bg-primary px-5 text-sm font-semibold text-primary-foreground" onClick={onClose}>Got it</button></>}</section></div>
}

function Footer() {
  return <footer className="border-t border-border bg-secondary/20 py-12"><div className="container-shell flex flex-col gap-8 sm:flex-row sm:items-end sm:justify-between"><div><Link href="/" className="focus-ring inline-flex items-center gap-2 text-sm font-semibold"><span className="grid size-7 place-items-center rounded-md bg-primary text-primary-foreground"><Sparkles size={14} /></span> ClearTrade AI</Link><p className="mt-4 max-w-xs text-sm leading-6 text-muted">Compliance infrastructure for institutions that refuse to stand still.</p></div><div className="flex flex-wrap gap-x-6 gap-y-3 text-sm text-muted"><Link className="focus-ring hover:text-foreground" href="/#platform">Platform</Link><Link className="focus-ring hover:text-foreground" href="/#workflow">Workflow</Link><Link className="focus-ring hover:text-foreground" href="/contact#contact-top">Contact</Link></div></div></footer>
}

function ClosingCta({ onOpen }: { onOpen: (type: Exclude<ModalType, null>) => void }) {
  return <section className="border-y border-border py-20 sm:py-28"><div className="container-shell"><div className="glass relative overflow-hidden rounded-2xl px-6 py-12 sm:px-12 sm:py-16"><div className="hero-grid pointer-events-none absolute inset-0 opacity-50" /><div className="relative max-w-2xl"><p className="eyebrow">A clearer next step</p><h2 className="mt-4 text-balance text-4xl font-semibold tracking-[-.04em] sm:text-5xl">Make your next review<br /><span className="text-primary">the easy one.</span></h2><p className="mt-5 max-w-xl text-lg leading-8 text-muted">Bring your hardest workflow. We&apos;ll show you how ClearTrade turns review pressure into operating confidence.</p><div className="mt-8 flex flex-wrap gap-3"><button className="focus-ring inline-flex h-12 items-center gap-2 rounded-lg bg-primary px-5 text-sm font-semibold text-primary-foreground" onClick={() => onOpen('join')}>Join Program <ArrowRight size={16} /></button><button className="focus-ring inline-flex h-12 items-center gap-2 rounded-lg border border-border px-5 text-sm font-semibold hover:bg-secondary" onClick={() => onOpen('learn')}>Learn More</button></div></div></div></div></section>
}

export default function ContactPage() {
  const [modal, setModal] = useState<ModalType>(null)

  return <main id="contact-top" className="min-h-screen bg-background px-4 py-8"><div className="container-shell"><Link href="/" className="focus-ring inline-flex items-center gap-2 text-sm text-muted hover:text-foreground"><ArrowLeft size={16} /> Back to ClearTrade</Link><div id="contact-form" className="mx-auto max-w-xl py-24 sm:py-32"><div className="grid size-12 place-items-center rounded-xl bg-primary text-primary-foreground"><Sparkles size={20} /></div><p className="eyebrow mt-8">Talk to Marcus</p><h1 className="mt-4 text-5xl font-semibold tracking-[-.05em]">Let&apos;s make review<br /><span className="text-primary">feel lighter.</span></h1><p className="mt-6 text-lg leading-8 text-muted">Tell us where your team spends the most time today. We&apos;ll bring a focused walkthrough and a point of view.</p><form className="mt-10 grid gap-4" onSubmit={(event) => event.preventDefault()}><input aria-label="Your name" required placeholder="Your name" className="focus-ring h-13 rounded-lg border border-input bg-secondary/40 px-4 text-foreground placeholder:text-muted-foreground" /><input aria-label="Work email" required type="email" placeholder="Work email" className="focus-ring h-13 rounded-lg border border-input bg-secondary/40 px-4 text-foreground placeholder:text-muted-foreground" /><textarea aria-label="How can we help?" placeholder="How can we help?" rows={4} className="focus-ring rounded-lg border border-input bg-secondary/40 px-4 py-3 text-foreground placeholder:text-muted-foreground" /><button className="focus-ring inline-flex h-13 items-center justify-center gap-2 rounded-lg bg-primary px-5 text-sm font-semibold text-primary-foreground">Start the conversation <ArrowRight size={16} /></button></form><p className="mt-5 text-xs leading-5 text-muted-foreground">Prototype contact form — connect to your preferred scheduling or CRM workflow before launch.</p></div></div><ClosingCta onOpen={setModal} /><Footer />{modal && <ProgramModal type={modal} onClose={() => setModal(null)} />}</main>
}
